# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# ---- Virtual envs / vendored deps ----
.venv/
venv/
env/
# isolated `pip --target` dir used when reusing an existing env
deps/
# per-model isolated venvs (coqui-tts, supertonic, …)
envs/

# ---- Generated pipeline outputs ----
# intermediate WAVs (MP3s are the kept deliverable)
results/audio/*.wav
# LibreOffice/spreadsheet lock files
.~lock.*#
# To keep the repo lean you may also ignore all generated results:
# results/audio/
# results/results.csv
# results/report.md

# ---- Model / dataset caches ----
.cache/
hf_cache/
*.gguf

# ---- OS / editor ----
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# direct-GPU comparison run
results_gpu/audio/*.wav

results/omnivoice/csv/omnivoice_optimization_results_backup.csv
results/moss-tts-nano/csv/moss_tts_nano_result_backup.csv
